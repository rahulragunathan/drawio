# Development and authoring tooling. Not needed to build or validate a diagram.
pytest>=8.0
ruff>=0.16

# preview.py only. The offline preview renderer is the one part of the skill
# with a third-party dependency; SKILL.md calls this out, and render_png.py is
# the fallback wherever matplotlib is unavailable.
matplotlib>=3.8
